# ADR-0003：设备指纹和并发风险只观察

- 状态：Accepted
- 日期：2026-08-16

## 背景

设备指纹可被重置或伪造，并发峰值也可能来自流式请求和异步任务。直接自动暂停容易误伤成员。

## 决策

风险聚合输出 `NORMAL`、`WATCH`、`LIMITED`、`SUSPEND`，其中 `SUSPEND` 只是人工暂停建议。
风险模块不得调用 Coordinator、修改 Seat 或禁用 Key。暂停必须是独立、可审计的人工命令。

原始指纹仅在请求处理期间存在，聚合结构只保留 HMAC 摘要。
